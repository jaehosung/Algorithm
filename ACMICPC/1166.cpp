#include<iostream>
#include<string>
#include<vector>
using namespace std;


bool myfunc(int v, int u){
    return strcmp(s.c_str()+u,s.c_:
}
int main(){
    string s;
    cin >>s;
    vector<int> vec;
    for(int i = 0; i<s.size(); i ++){ vec.push(i); }

    sort(vec.begin(),vec.begin,myfunc);
    return 0;
}
